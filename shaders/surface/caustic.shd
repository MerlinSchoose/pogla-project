#version 450

layout( triangles ) in;

layout( line, max_vertices=2 ) out;

in vec3 vNormal[3];
float depth[3];
float cameraDepth[3];

void main() {
    vec3 n = normalize((vNormal[0] + vNormal[1] + vNormal[2]) / 3.0);
    vec3 p = (gl_in[0] + gl_in[1] + gl_in[2]) / 3.0;

}